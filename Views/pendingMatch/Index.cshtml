@model IEnumerable<Hieromemics.Models.pendingMatch>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.lookingId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.seekingId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.lookingId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.seekingId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.pendingId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.pendingId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.pendingId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
